steps:
  # Build the container image
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', 'gcr.io/$PROJECT_ID/slideflip-backend:$COMMIT_SHA', '.']
  
  # Push the container image to Container Registry
  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'gcr.io/$PROJECT_ID/slideflip-backend:$COMMIT_SHA']
  
  # Deploy container image to Cloud Run
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
    - 'run'
    - 'deploy'
    - 'slideflip-backend'
    - '--image'
    - 'gcr.io/$PROJECT_ID/slideflip-backend:$COMMIT_SHA'
    - '--region'
    - 'us-central1'
    - '--platform'
    - 'managed'
    - '--allow-unauthenticated'
    - '--port'
    - '8080'
    - '--memory'
    - '2Gi'
    - '--cpu'
    - '2'
    - '--timeout'
    - '900'
    - '--concurrency'
    - '1000'
    - '--max-instances'
    - '10'
    - '--set-env-vars'
    - 'DEBUG=false,HOST=0.0.0.0,PORT=8080'
    - '--set-secrets'
    - 'OPENAI_API_KEY=openai-api-key:latest'

images:
  - gcr.io/$PROJECT_ID/slideflip-backend:$COMMIT_SHA