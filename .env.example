# SlideFlip environment example
# Copy this file to `.env.local` at the project root for the frontend
# and to `backend/.env.local` for the Python backend.

# --- Frontend (Next.js) ---
# Required: Supabase client credentials
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Required: Backend endpoints used by the frontend
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000
NEXT_PUBLIC_BACKEND_WS_URL=ws://localhost:8000

# Optional: Vercel runtime envs (set automatically on Vercel)
# VERCEL_URL=
# VERCEL_ENV=
# VERCEL_PROJECT_PRODUCTION_URL=

# --- Shared APIs ---
# Required for AI features (used by frontend server routes and backend)
OPENAI_API_KEY=

# Optional: Research provider used by /api/research
TAVILY_API_KEY=

# --- LDAP (optional; enable if using corporate LDAP auth) ---
LDAP_URL=ldap://your-ldap-server:389
LDAP_BASE_DN=dc=yourcompany,dc=com
LDAP_BIND_DN=cn=admin,dc=yourcompany,dc=com
LDAP_BIND_PASSWORD=your-admin-password
LDAP_USER_SEARCH_BASE=ou=users,dc=yourcompany,dc=com
LDAP_USER_SEARCH_FILTER=(uid={username})
LDAP_GROUP_SEARCH_BASE=ou=groups,dc=yourcompany,dc=com
LDAP_GROUP_SEARCH_FILTER=(member={userDn})

# --- Scripts / local admin (do NOT expose to browsers) ---
# Used only by Node scripts like scripts/setup-slide-templates.js
SUPABASE_SERVICE_ROLE_KEY=

# --- Backend (FastAPI) overrides (optional; defaults shown) ---
# Place these mainly in backend/.env.local if you want to override
HOST=0.0.0.0
PORT=8000
DEBUG=True
SECRET_KEY=change-me
UPLOAD_DIR=uploads
MAX_FILE_SIZE=52428800
